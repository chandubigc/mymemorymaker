import _ from 'lodash';
import { createUUID } from '../../Utils/CreateUUID';

export const messageProcessor = (payload, mapFromHistory = {}) => {};

export const broadCastMsgProcessor = (payload, mapFromHistory = {}) => {};
